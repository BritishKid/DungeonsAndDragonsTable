import Objects.PCClass;

public class ReadFromJson {

    //don't like this class could be read interface with overides or something

    //gets random object from a specific JSON containing a list
    public String getRandomFromJSONList(String jsonPath) {
        String randomValue = "Failed Mock";

        return randomValue;
    }

    //gets random object from a specific JSON containing a list at node
    public String getRandomFromJSONList(String jsonPath, String node) {

        return "Failed Mock";
    }

    //reads from class.json and read out all the informatino
    public PCClass getClassInformation(String classToRead) {
        return null;
    }
}
